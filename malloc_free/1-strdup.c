#include "main.h"

/**
 * 
 * 
 * 
 * 
 * 
 * 
 */

char *_strdup(char *str)
{
	unsigned int i;
	char *tab;

		if(str == NULL)
	{
		
		return(NULL);
	}

	tab = malloc(sizeof(char));

	for (i = i + 1; i != '\0'; i++)
	{
		tab[i] = str;
	}
	
	if(str == NULL)
	{
		
		return(NULL);
	}


}